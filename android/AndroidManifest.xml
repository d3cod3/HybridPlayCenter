<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hybridplay.center"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="22" />
    
    <!-- Phone Permissions -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Internal Memory / External SD Card Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_LOCAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_LOCAL_STORAGE" />
    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- BLUETOOTH Permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"/>
    
    <!-- Declare this required feature if you want to make the APP available to BLE-capable
    devices only.  If you want to make your APP available to devices that don't support BLE,
    you should omit this in the manifest.  Instead, determine BLE capability by using
    PackageManager.hasSystemFeature(FEATURE_BLUETOOTH_LE) -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar.Fullscreen" >
        <activity
            android:name=".HP_Home"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".HP_Games" android:theme="@style/HPTheme" android:configChanges="keyboardHidden|orientation|screenSize"></activity>
        <activity android:name=".HP_Bluetooth" android:theme="@style/HPTheme" android:configChanges="keyboardHidden|orientation|screenSize"></activity>
        <activity android:name=".HP_Calibrator" android:theme="@style/HPTheme"></activity>
        <activity android:name=".HP_Community" android:theme="@style/HPTheme"></activity>
        <activity android:name=".HP_Credits" android:theme="@style/HPTheme"></activity>
        <activity android:name=".HP_Instructions" android:theme="@style/HPTheme"></activity>
        <activity android:name=".HP_Drawer" android:theme="@style/HPTheme"></activity>
        
        <service android:name="com.hybridplay.bluetooth.BluetoothLeService" android:enabled="true"/>
        
    </application>

</manifest>
